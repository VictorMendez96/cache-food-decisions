{{!-- <div class="card">
    <div class="card-header">
        Thanks for Visiting! Log In Again? 
    </div>

    {{!-- The form already sends them back to the homepage/login page, do we need this too? --}}
    <div class="card-body"></div>
      <form id="logout-form">
        <div class="mb-3">
          <label for="logout-email" class="form-label">Email address</label>
          <input type="email" class="form-control" id="logout-email" aria-describedby="emailHelp">
          <div id="emailHelp" class="form-text">We'll never share your email with anyone else.</div>
        </div>
        <div class="mb-3">
          <label for="logout-password" class="form-label">Password</label>
          <input type="password" class="form-control" id="logout-password">
        </div>
        <button type="submit" class="btn btn-primary">Log In!</button>
      </form>    
    </div>
</div>

<script src="./javascript/logout.js"></script> --}}